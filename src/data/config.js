// Configuração pública do jogo. Nada aqui é segredo.
// URL do Realtime Database do Firebase (sem barra no fim). O acesso é
// controlado pelas rules do RTDB (exigem um código de família >= 8 chars),
// não por chave.
export const FIREBASE_DB_URL = 'https://game-mistura-default-rtdb.firebaseio.com';
